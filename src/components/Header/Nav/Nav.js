import React from "react";
import StyledNav from "./StyledNav";

const Nav = (props) => {
  return (
    <StyledNav>
      <a href="#">Work</a>
      <a href="#">Me</a>
      <a href="#">Contact</a>
    </StyledNav>
  );
};

export default Nav;
